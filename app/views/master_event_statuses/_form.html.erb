<%= form_for(@master_event_status) do |f| %>
  <% if @master_event_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@master_event_status.errors.count, "error") %> prohibited this master_event_status from being saved:</h2>

      <ul>
      <% @master_event_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
