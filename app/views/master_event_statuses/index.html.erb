<h1>Listing master_event_statuses</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @master_event_statuses.each do |master_event_status| %>
      <tr>
        <td><%= link_to 'Show', master_event_status %></td>
        <td><%= link_to 'Edit', edit_master_event_status_path(master_event_status) %></td>
        <td><%= link_to 'Destroy', master_event_status, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Master event status', new_master_event_status_path %>
